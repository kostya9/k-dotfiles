if (Test-Path ~\.ideavimrc)
{
	rm ~\.ideavimrc
}

if (Test-Path ~\.config\nvim)
{
	rm ~\.config\nvim
}

if (Test-Path ~\.vimrc)
{
	rm ~\.vimrc
}

if (Test-Path ~\.config\whkdrc)
{
	rm ~\.config\whkdrc
}

if (Test-Path ~\komorebi.json)
{
	rm ~\komorebi.json
}